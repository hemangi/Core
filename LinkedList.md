### Linked List 
1) Linked List is a linear data structure.
2) Elements are not stored at a contiguous location; the elements are linked using pointers.
3) Linked list consists of nodes where each node contains a data field and a reference(link) to the next node in the list.
